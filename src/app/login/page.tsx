import { LoginView } from "@/components/features/auth/login-view";

export const metadata = {
  title: "Login - GiziSync",
  description: "Masuk ke sistem informasi Posyandu GiziSync",
};

export default function LoginPage() {
  return <LoginView />;
}